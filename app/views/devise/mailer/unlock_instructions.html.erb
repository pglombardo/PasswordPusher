<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8">

  <title><%= content_for?(:html_title) ? yield(:html_title) : Settings.brand.title %></title>

  <meta content="<%= _('Password Pusher is an application to securely send passwords over the web. Links to passwords expire after a certain number of views and/or time has passed.') %>" name='description'>
  <meta content='width=device-width, initial-scale=1' name='viewport'>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
</head>

<body class="d-flex flex-column min-vh-100">

<div class="container">

<h1><%= _('Unlock Your Account') %></h1>

<p>
<%= _('Your account has been locked due to an excessive number of unsuccessful sign in attempts.') %>
</p>

<p>
<%= link_to _('Click this link'), unlock_url(@resource, unlock_token: @token) %>
<%= _('to unlock your account or by copying and pasting the following URL into your browser:') %>
</p>

<pre class="chroma">
<%= unlock_url(@resource, unlock_token: @token) %>
</pre>
</p>
<%= render 'shared/email_footer' %>

</div>

</body>
</html>
