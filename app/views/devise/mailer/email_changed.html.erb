<p>Hello,</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p>We're contacting you to notify you that your email at <a href="<%= root_url %>">Password Pusher</a> is being changed to <%= @resource.unconfirmed_email %>.</p>
<% else %>
  <p>We're contacting you to notify you that your email at <a href="<%= root_url %>">Password Pusher</a> has been changed to <%= @resource.email %>.</p>
<% end %>

<%= render 'shared/email_footer' %>