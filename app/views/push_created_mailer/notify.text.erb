<%= _('You have received a Push from %{user_email}') % { user_email: @push.user&.email.presence || _("Someone")} %>

<%= _('Please find the secret link below to access the sensitive information.') %>

<%= _('Secret link:') %> <%= @secret_url %>
<%= _('Important Notes:') %>

<%= _('This link is valid for %{remaining}, or until %{views} views, whichever occurs first.') % { remaining: format_time_remaining(@push), views: @push.expire_after_views } %>
<%= _('For added security, you may be prompted to enter a passphrase (provided separately) to access the content.') %>
<%= _('Once retrieved, the link and content will be deleted upon expiration.') %>

<%= _('This email may contain confidential information intended only for the recipient.') %>
<%= _('If you received this in error, please delete it and notify us immediately.') %>
